<div class="h-entry">
  <h2 class="p-name">
    <%# TODO: make name dynamical, e.g. New Client, Updated Client, Lost Client (or something) %>
    New Client:
    <strong><%= link_to client.name, entry_url(client), class: "u-uid u-url", rel: "bookmark" %></strong>
  </h2>

  <%# TODO: make summary dynamical, e.g. signed up, updated, left, (or something) %>
  <p class="p-summary"><%= "#{client.name} signed up with #{client.locations.size} locations!" %></p>

  <p>Updated at
    <time datetime="<%= client.updated_at_computer_readable %>" class="dt-updated">
      <%= client.updated_at_human_readable %>
    </time>
  </p>

  <% unless client.updated_at_computer_readable == client.updated_at_computer_readable %>
    <p>Published at
      <time datetime="<%= client.created_at_computer_readable %>" class="dt-published">
        <%= client.created_at_human_readable %>
      </time>
    </p>
  <% end %>

  <%# TODO: author should ideally be the person that performed the action %>
  <p>by
    <span class="p-author h-card">
      <%= link_to "g5-hub", root_url, class: "u-uid u-url" %>
    </span>
  </p>

  <%# TODO: make category dynamical, e.g. created, updated, destroyed (or something) %>
  <p>
    <%= link_to "create-client", tag_url("create-client"), class: "p-category", rel: "tag" %>
  </p>

  <hr/>

  <div class="e-content">
    <%= render client %>
  </div>
</div>
